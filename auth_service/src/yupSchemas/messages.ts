export const tooLong = (length: number, name: string) =>
  `${name} must be at most ${length} characters in length`;
export const tooShort = (length: number, name: string) =>
  `${name} must be at least ${length} characters in length`;
export const invalidId = 'Invalid ID';
